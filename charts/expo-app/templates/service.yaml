apiVersion: v1
kind: Service
metadata:
  name: {{ include "expo-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "expo-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
